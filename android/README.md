# android — Jetpack Compose app

Thin UI over the shared Go core (`core/`, embedded as an `.aar` via gomobile).
Distributed as an APK / via Firebase App Distribution.

Screens:
- Field tester: one pass/fail/inconclusive verdict + three sub-results, no jargon.
- Engineer detail: per-run telemetry, links to dashboard.

Leads the app work (M2): easiest to distribute and to mark packets. `IP_TOS` via
NDK; capture-verify ECT(1)/NQB survival on a clean path.

Status: not started. First built in M2.
